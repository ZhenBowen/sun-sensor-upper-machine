# EB90 18字节测试协议 修改方案

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在保留旧 26 字节 EB90 协议代码的基础上，新增 18 字节无帧头测试协议的支持，使模拟器和串口均可使用该协议收发数据；同时将界面上的 X/Y 标签改为 Alpha/Beta。

**Architecture:** 新增 `EB90TestParser`、`parse_eb90_test_frame`、`pack_eb90_test_frame` 函数和 `SimulatorThread` 的 eb90_test 分支；保留旧代码并加注释标注；`SpotTelemetry` 增加 ADC 真实字段并改 `signal_sum`/`rx`/`ry` 为实际计算；界面标签 X/Y → Alpha/Beta。

**Tech Stack:** Python 3.11, PyQt5, pyqtgraph, pyserial, unittest

---

## 帧格式定义

### 旧 EB90 26 字节格式（保留，不删除）

| 偏移 | 字节 | 字段 | 类型 | 说明 |
|---|---|---|---|---|
| 0–1 | 2 | SOF | — | 固定 `EB 90` |
| 2 | 1 | sun_present | uint8 | 太阳存在标志 |
| 3–6 | 4 | x (alpha) | float32 BE | 光斑X / alpha角 |
| 7–10 | 4 | y (beta) | float32 BE | 光斑Y / beta角 |
| 11–12 | 2 | I_AX1 | uint16 BE | 第一路ADC |
| 13–14 | 2 | I_AX2 | uint16 BE | 第二路ADC |
| 15–16 | 2 | I_AY1 | uint16 BE | 第三路ADC |
| 17–18 | 2 | I_AY2 | uint16 BE | 第四路ADC |
| 19 | 1 | 温度 | uint8 | — |
| 20–21 | 2 | 阈值 | uint16 | — |
| 22 | 1 | 增益 | uint8 | — |
| 23 | 1 | 错误计数 | uint8 | — |
| 24 | 1 | 节点ID | uint8 | — |
| 25 | 1 | checksum | uint8 | sum(byte 0–24) & 0xFF |

### 新 EB90 18 字节测试格式（新增）

| 偏移 | 字节 | STM32代码 | 字段 | 类型 | 说明 |
|---|---|---|---|---|---|
| 0 | 1 | `msg[0] = right` | sun_present | uint8 | 00=无太阳，01=检测到 |
| 1–4 | 4 | `msg[1..4]` | alpha (x) | float32 BE | 太阳入射alpha角 |
| 5–8 | 4 | `msg[5..8]` | beta (y) | float32 BE | 太阳入射beta角 |
| 9–10 | 2 | `msg[9..10]` | I_AX1 | uint16 BE | AD_DMA[0]，X轴一路ADC |
| 11–12 | 2 | `msg[11..12]` | I_AX2 | uint16 BE | AD_DMA[1]，X轴二路ADC |
| 13–14 | 2 | `msg[13..14]` | I_AY1 | uint16 BE | AD_DMA[2]，Y轴一路ADC |
| 15–16 | 2 | `msg[15..16]` | I_AY2 | uint16 BE | AD_DMA[3]，Y轴二路ADC |
| 17 | 1 | `msg[17]` | checksum | uint8 | sum(byte 0–16) & 0xFF |

**共 18 字节，无帧头，校验和不含自身。**

> **WARNING:** 此格式无帧头同步，纯靠校验和 + sun_present(仅允许0x00/0x01)定位帧边界。随机噪声通过校验和的概率约 1/12800，仅用于临时联调测试，生产环境必须使用带帧头的协议。

---

## 修改文件清单

| 文件 | 改动类型 | 说明 |
|---|---|---|
| `sun_models.py` | 修改 | SpotTelemetry 增加 ADC 字段，改 signal_sum/rx/ry 为真实计算 |
| `sun_protocol.py` | 修改 | 保留旧26字节代码+注释，新增18字节：常量、结构体、解析/打包函数、EB90TestParser |
| `sun_simulator.py` | 修改 | 新增 `next_eb90_test_frame()` 方法，import pack_eb90_test_frame |
| `sun_host.py` | 修改 | SerialThread 和 SimulatorThread 支持 "eb90_test" 协议路由 |
| `sun_gui.py` | 修改 | 协议下拉新增选项；状态栏 x/y → alpha/beta；simulator 传 protocol |
| `sun_monitor.py` | 修改 | 图表标题和轴标签 X/Y → Alpha/Beta；数值字段名 x/y → alpha/beta |
| `tests/test_sun_protocol.py` | 修改 | 新增 EB90 18字节测试用例 |

---

## 逐文件详细修改

### Task 1: 修改 `sun_models.py` — SpotTelemetry 增加 ADC 字段

**Files:**
- Modify: `sun_models.py` (lines 91–167, SpotTelemetry class)

- [ ] **Step 1: 将 adc_vax1/vax2/vay1/vay2 从 property 改为 dataclass 字段，改 signal_sum/rx/ry 为真实计算**

将当前 SpotTelemetry 类（第91–167行）替换为：

```python
@dataclass(frozen=True)
class SpotTelemetry:
    sun_present: int
    x: float
    y: float
    raw_frame_hex: str = ""
    seq: int = 0
    node_id: int = 0
    timestamp_ms: int = 0
    status_word: int = 0
    saturation_flag: int = 0
    adc_vax1: int = 0
    adc_vax2: int = 0
    adc_vay1: int = 0
    adc_vay2: int = 0
    received_time_ms: int = field(default_factory=now_ms)

    @property
    def spot_x(self) -> float:
        return self.x

    @property
    def spot_y(self) -> float:
        return self.y

    @property
    def alpha_deg(self) -> float:
        return self.x

    @property
    def beta_deg(self) -> float:
        return self.y

    @property
    def temp_c(self) -> float:
        return 0.0

    @property
    def valid_flag(self) -> bool:
        return bool(self.sun_present)

    @property
    def signal_sum(self) -> int:
        return self.adc_vax1 + self.adc_vax2 + self.adc_vay1 + self.adc_vay2

    @property
    def rx(self) -> float:
        denom = self.adc_vax1 + self.adc_vax2
        if denom <= 0:
            return 0.0
        return (self.adc_vax2 - self.adc_vax1) / denom

    @property
    def ry(self) -> float:
        denom = self.adc_vay1 + self.adc_vay2
        if denom <= 0:
            return 0.0
        return (self.adc_vay2 - self.adc_vay1) / denom

    @property
    def status_flags(self) -> Dict[str, bool]:
        return {
            "VALID": bool(self.sun_present),
            "SUN_PRESENT": bool(self.sun_present),
            "SATURATED": False,
            "LOW_SIGNAL": not bool(self.sun_present),
            "ADC_ERROR": False,
            "TEMP_WARNING": False,
            "CALIBRATION_MODE": False,
            "PARAM_ERROR": False,
        }
```

关键变化：
- `adc_vax1`~`adc_vay2` 从 `@property` 返回常量 0 变为 `dataclass` 字段（默认值 0）
- `signal_sum` 从返回 0 变为真实计算
- `rx` 从返回 `self.x` 变为用 ADC 归一化计算
- `ry` 从返回 `self.y` 变为用 ADC 归一化计算
- **旧 26 字节解析仍兼容**：`parse_eb90_frame` 不填 ADC 字段，默认为 0，`signal_sum`=0，`rx`=`ry`=0（因为 denom=0 回退到 0.0）

- [ ] **Step 2: 运行现有测试确认无回归**

Run: `python tests\run_tests.py`

---

### Task 2: 修改 `sun_protocol.py` — 新增 18 字节协议

**Files:**
- Modify: `sun_protocol.py`

- [ ] **Step 1: 在 EB90_SOF 行前加注释标注旧格式**

在 `EB90_SOF = b"\xEB\x90"` 行（第17行）前加注释：

```python
# ---------------------------------------------------------------------------
# EB90 26-byte format (original version, with SOF header)
# Used by the original sun sensor hardware.
# Frame: EB 90 + sun_present(1) + x(4 float BE) + y(4 float BE)
#         + I_AX1(2) + I_AX2(2) + I_AY1(2) + I_AY2(2)
#         + temp(1) + threshold(2) + gain(1) + err_cnt(1) + node_id(1)
#         + checksum(1) = 26 bytes
# Checksum: sum(byte[0:25]) & 0xFF
# ---------------------------------------------------------------------------
```

- [ ] **Step 2: 在 EB90_FRAME_LEN 之后新增 18 字节常量**

在 `EB90_FRAME_LEN = 26` 之后新增：

```python
# ---------------------------------------------------------------------------
# EB90 18-byte test format (temporary, NO SOF header)
# WARNING: No frame header for byte-stream synchronization.
# Frame sync relies on checksum + sun_present filtering only.
# Use ONLY for temporary hardware testing, NOT for production.
# Frame: sun_present(1) + alpha(4 float BE) + beta(4 float BE)
#         + I_AX1(2 uint16 BE) + I_AX2(2 uint16 BE) + I_AY1(2) + I_AY2(2)
#         + checksum(1) = 18 bytes
# Checksum: sum(byte[0:17]) & 0xFF == byte[17]
# sun_present must be 0x00 or 0x01.
# ---------------------------------------------------------------------------
EB90_TEST_FRAME_LEN = 18
_EB90_TEST_PAYLOAD = struct.Struct(">f f H H H H")  # alpha beta I_AX1 I_AX2 I_AY1 I_AY2
```

- [ ] **Step 3: 新增 parse_eb90_test_frame() 和 pack_eb90_test_frame() 函数**

在 `pack_eb90_frame()` 函数之后（约第156行之后）新增：

```python
def parse_eb90_test_frame(frame: bytes) -> SpotTelemetry:
    if len(frame) != EB90_TEST_FRAME_LEN:
        raise ProtocolError(f"invalid EB90 test frame length: {len(frame)}")
    if frame[0] not in (0, 1):
        raise ProtocolError(f"invalid EB90 test sun_present: {frame[0]:#04x}")
    expected_checksum = sum(frame[:-1]) & 0xFF
    actual_checksum = frame[-1]
    if actual_checksum != expected_checksum:
        raise BadCrcError(
            f"bad EB90 test checksum: expected 0x{expected_checksum:02X}, got 0x{actual_checksum:02X}"
        )
    sun_present = frame[0]
    alpha, beta, adc_vax1, adc_vax2, adc_vay1, adc_vay2 = _EB90_TEST_PAYLOAD.unpack(frame[1:17])
    return SpotTelemetry(
        sun_present=sun_present,
        x=alpha,
        y=beta,
        adc_vax1=adc_vax1,
        adc_vax2=adc_vax2,
        adc_vay1=adc_vay1,
        adc_vay2=adc_vay2,
        raw_frame_hex=frame.hex(" ").upper(),
    )


def pack_eb90_test_frame(
    sun_present: int,
    alpha: float,
    beta: float,
    adc_vax1: int,
    adc_vax2: int,
    adc_vay1: int,
    adc_vay2: int,
) -> bytes:
    payload = _EB90_TEST_PAYLOAD.pack(
        float(alpha), float(beta),
        adc_vax1 & 0xFFFF, adc_vax2 & 0xFFFF,
        adc_vay1 & 0xFFFF, adc_vay2 & 0xFFFF,
    )
    body = bytes([sun_present & 0x01]) + payload
    checksum = sum(body) & 0xFF
    return body + bytes([checksum])
```

- [ ] **Step 4: 新增 EB90TestParser 类**

在 `EB90Parser` 类之后（约第318行之后）新增：

```python
class EB90TestParser:
    # WARNING: No SOF header. Frame sync relies on checksum + sun_present filter.
    # Random noise passes checksum with ~1/12800 probability (sun_present + checksum).
    # This parser is for temporary testing ONLY.
    def __init__(self) -> None:
        self._buffer = bytearray()
        self.crc_error_count = 0
        self.frame_error_count = 0
        self.drop_count = 0
        self.frame_count = 0
        self.byte_count = 0

    def feed(self, data: bytes) -> List[SpotTelemetry]:
        if not data:
            return []
        self.byte_count += len(data)
        self._buffer.extend(data)
        parsed: List[SpotTelemetry] = []

        while len(self._buffer) >= EB90_TEST_FRAME_LEN:
            candidate = bytes(self._buffer[:EB90_TEST_FRAME_LEN])
            if candidate[0] not in (0, 1):
                self.frame_error_count += 1
                del self._buffer[0]
                continue
            try:
                telemetry = parse_eb90_test_frame(candidate)
                del self._buffer[:EB90_TEST_FRAME_LEN]
                self.frame_count += 1
                parsed.append(telemetry)
                continue
            except BadCrcError:
                self.crc_error_count += 1
                del self._buffer[0]
                continue
            except ProtocolError:
                self.frame_error_count += 1
                del self._buffer[0]
                continue

        return parsed

    def stats(self, frame_rate_hz: float = 0.0) -> TelemetryStats:
        return TelemetryStats(
            frame_rate_hz=frame_rate_hz,
            drop_count=self.drop_count,
            crc_error_count=self.crc_error_count,
            frame_count=self.frame_count,
            byte_count=self.byte_count,
        )
```

- [ ] **Step 5: 运行现有 EB90 测试确认无回归**

Run: `python -m unittest tests\test_sun_protocol.py -v`

---

### Task 3: 修改 `sun_simulator.py` — 新增 18 字节模拟帧生成

**Files:**
- Modify: `sun_simulator.py`

- [ ] **Step 1: 替换 import 行**

将：
```python
from sun_protocol import pack_telemetry
```
改为：
```python
from sun_protocol import pack_eb90_test_frame, pack_telemetry
```

- [ ] **Step 2: 在 next_frame() 方法之后新增 next_eb90_test_frame() 方法**

在 `next_frame()` 方法后（约第74行之后）新增：

```python
    def next_eb90_test_frame(self) -> bytes:
        telemetry = self.next_telemetry()
        frame = bytearray(
            pack_eb90_test_frame(
                sun_present=telemetry.sun_present & 0x01,
                alpha=telemetry.alpha_deg,
                beta=telemetry.beta_deg,
                adc_vax1=telemetry.adc_vax1,
                adc_vax2=telemetry.adc_vax2,
                adc_vay1=telemetry.adc_vay1,
                adc_vay2=telemetry.adc_vay2,
            )
        )
        if self.mode == "crc_error" and self.crc_error_every > 0 and self._frame_index % self.crc_error_every == 0:
            frame[-1] ^= 0xFF
        return bytes(frame)
```

---

### Task 4: 修改 `sun_host.py` — 新增协议路由

**Files:**
- Modify: `sun_host.py`

- [ ] **Step 1: import 行新增 EB90TestParser**

将：
```python
from sun_protocol import EB90Parser, TelemetryParser, hex_text_to_bytes, pack_command
```
改为：
```python
from sun_protocol import EB90Parser, EB90TestParser, TelemetryParser, hex_text_to_bytes, pack_command
```

- [ ] **Step 2: SerialThread._make_parser() 新增 eb90_test 分支**

将：
```python
    def _make_parser(self):
        if self.protocol == "eb90":
            return EB90Parser()
        return TelemetryParser()
```
改为：
```python
    def _make_parser(self):
        if self.protocol == "eb90":
            return EB90Parser()
        if self.protocol == "eb90_test":
            return EB90TestParser()
        return TelemetryParser()
```

- [ ] **Step 3: SimulatorThread 新增 protocol 参数和 _make_parser 方法**

将 SimulatorThread 整体替换为：

```python
class SimulatorThread(_TelemetryThread):
    def __init__(self, node_id: int = 1, rate_hz: float = 10.0, mode: str = "normal", protocol: str = "recommended") -> None:
        super().__init__()
        self.node_id = node_id
        self.rate_hz = rate_hz
        self.mode = mode
        self.protocol = protocol

    def run(self) -> None:
        self._running = True
        simulator = SunSimulator(node_id=self.node_id, rate_hz=self.rate_hz, mode=self.mode)
        parser = self._make_parser()
        meter = RateMeter()
        interval_ms = max(1, int(round(1000.0 / max(self.rate_hz, 0.1))))
        self.status_changed.emit(f"Simulator running: {self.mode}, {self.rate_hz:.1f} Hz, protocol={self.protocol}")

        while self._running:
            if self.protocol == "eb90_test":
                frame = simulator.next_eb90_test_frame()
            else:
                frame = simulator.next_frame()
            self.raw_bytes_received.emit(frame)
            for telemetry in parser.feed(frame):
                rate = meter.mark()
                stats = parser.stats(frame_rate_hz=rate)
                self.stats_updated.emit(stats)
                self.telemetry_received.emit(telemetry)
            self.msleep(interval_ms)

        self.status_changed.emit("Simulator stopped")

    def _make_parser(self):
        if self.protocol == "eb90":
            return EB90Parser()
        if self.protocol == "eb90_test":
            return EB90TestParser()
        return TelemetryParser()
```

- [ ] **Step 4: SunHost.start_simulator() 传递 protocol 参数**

将：
```python
    def start_simulator(self, node_id: int = 1, rate_hz: float = 10.0, mode: str = "normal") -> None:
        self.stop()
        self._thread = SimulatorThread(node_id=node_id, rate_hz=rate_hz, mode=mode)
        self._connect_thread_signals(self._thread)
        self._thread.start()
```
改为：
```python
    def start_simulator(self, node_id: int = 1, rate_hz: float = 10.0, mode: str = "normal", protocol: str = "recommended") -> None:
        self.stop()
        self._thread = SimulatorThread(node_id=node_id, rate_hz=rate_hz, mode=mode, protocol=protocol)
        self._connect_thread_signals(self._thread)
        self._thread.start()
```

---

### Task 5: 修改 `sun_gui.py` — 新增协议选项 + Alpha/Beta 标签

**Files:**
- Modify: `sun_gui.py`

- [ ] **Step 1: 协议下拉框新增选项并重命名旧选项**

将：
```python
        self.protocol_combo = QComboBox()
        self.protocol_combo.addItem("Recommended 32-byte", "recommended")
        self.protocol_combo.addItem("EB90 spot x/y", "eb90")
```
改为：
```python
        self.protocol_combo = QComboBox()
        self.protocol_combo.addItem("Recommended 32-byte", "recommended")
        self.protocol_combo.addItem("EB90 26-byte (original)", "eb90")
        self.protocol_combo.addItem("EB90 18-byte (test)", "eb90_test")
```

- [ ] **Step 2: 模拟器启动时传递 protocol 参数**

将 `toggle_connection()` 中的：
```python
        else:
            mode_map = {
                "sim_normal": "normal",
                "sim_crc": "crc_error",
                "sim_drop": "drop_frame",
            }
            self.host.start_simulator(node_id=node_id, rate_hz=float(self.rate_spin.value()), mode=mode_map[source])
```
改为：
```python
        else:
            mode_map = {
                "sim_normal": "normal",
                "sim_crc": "crc_error",
                "sim_drop": "drop_frame",
            }
            protocol = self.protocol_combo.currentData()
            self.host.start_simulator(node_id=node_id, rate_hz=float(self.rate_spin.value()), mode=mode_map[source], protocol=protocol)
```

- [ ] **Step 3: 状态栏显示 alpha/beta**

将 `on_telemetry()` 中的：
```python
        self.statusBar().showMessage(
            f"x={telemetry.spot_x:.4f} y={telemetry.spot_y:.4f} sun={telemetry.sun_present} rate={self.latest_stats.frame_rate_hz:.2f} Hz"
        )
```
改为：
```python
        self.statusBar().showMessage(
            f"alpha={telemetry.spot_x:.4f} beta={telemetry.spot_y:.4f} sun={telemetry.sun_present} rate={self.latest_stats.frame_rate_hz:.2f} Hz"
        )
```

---

### Task 6: 修改 `sun_monitor.py` — 所有 X/Y 标签改为 Alpha/Beta

**Files:**
- Modify: `sun_monitor.py`

- [ ] **Step 1: 修改所有用户可见的 X/Y 标签**

共需修改以下位置：

1. **第42–44行 `fields` 列表**：`"x"` → `"alpha"`，`"y"` → `"beta"`
2. **第72行 XY图标题**：`"Spot Position X-Y"` → `"Spot Position Alpha-Beta"`
3. **第73行 XY图left轴标签**：`"y"` → `"Beta (\u00b0)"`
4. **第74行 XY图bottom轴标签**：`"x"` → `"Alpha (\u00b0)"`
5. **第85行 趋势图标题**：`"Spot X/Y Trend"` → `"Alpha/Beta Trend"`
6. **第86行 趋势图left轴标签**：`"position"` → `"Angle (\u00b0)"`
7. **第88–89行 曲线名**：`name="x"` → `name="alpha"`，`name="y"` → `name="beta"`
8. **第99–101行 values字典**：`"x"` → `"alpha"`，`"y"` → `"beta"`
9. **第113–114行 status_lines**：`f"x = ..."` → `f"alpha = ..."`，`f"y = ..."` → `f"beta = ..."`

---

### Task 7: 新增测试用例

**Files:**
- Modify: `tests/test_sun_protocol.py`

- [ ] **Step 1: 更新 import 行**

将：
```python
from sun_protocol import (
    EB90Parser,
    EB90_FRAME_LEN,
    TELEMETRY_FRAME_LEN,
    TelemetryParser,
    crc16_modbus,
    hex_text_to_bytes,
    pack_eb90_frame,
    pack_command,
    pack_telemetry,
    parse_eb90_frame,
    parse_telemetry_frame,
)
```
改为：
```python
from sun_protocol import (
    EB90Parser,
    EB90TestParser,
    EB90_FRAME_LEN,
    EB90_TEST_FRAME_LEN,
    TELEMETRY_FRAME_LEN,
    TelemetryParser,
    crc16_modbus,
    hex_text_to_bytes,
    pack_eb90_frame,
    pack_eb90_test_frame,
    pack_command,
    pack_telemetry,
    parse_eb90_frame,
    parse_eb90_test_frame,
    parse_telemetry_frame,
)
```

- [ ] **Step 2: 新增 EB90 18字节测试类**

在 `if __name__ == "__main__":` 之前新增：

```python
class TestEB90TestProtocol(unittest.TestCase):
    def test_pack_and_parse_round_trip(self):
        frame = pack_eb90_test_frame(
            sun_present=1, alpha=12.5, beta=-3.25,
            adc_vax1=2327, adc_vax2=2330, adc_vay1=2347, adc_vay2=2328,
        )
        self.assertEqual(len(frame), EB90_TEST_FRAME_LEN)
        self.assertEqual(frame[0], 1)
        checksum = sum(frame[:-1]) & 0xFF
        self.assertEqual(frame[-1], checksum)

        parsed = parse_eb90_test_frame(frame)
        self.assertEqual(parsed.sun_present, 1)
        self.assertAlmostEqual(parsed.x, 12.5, places=4)
        self.assertAlmostEqual(parsed.y, -3.25, places=4)
        self.assertEqual(parsed.adc_vax1, 2327)
        self.assertEqual(parsed.adc_vax2, 2330)
        self.assertEqual(parsed.adc_vay1, 2347)
        self.assertEqual(parsed.adc_vay2, 2328)

    def test_parse_rejects_bad_checksum(self):
        frame = bytearray(pack_eb90_test_frame(
            sun_present=1, alpha=1.0, beta=2.0,
            adc_vax1=100, adc_vax2=200, adc_vay1=300, adc_vay2=400,
        ))
        frame[-1] ^= 0xFF
        parser = EB90TestParser()

        parsed = parser.feed(bytes(frame))

        self.assertEqual(parsed, [])
        self.assertEqual(parser.crc_error_count, 1)

    def test_parser_handles_sticky_frames(self):
        frame_a = pack_eb90_test_frame(
            sun_present=1, alpha=1.25, beta=2.5,
            adc_vax1=100, adc_vax2=200, adc_vay1=300, adc_vay2=400,
        )
        frame_b = pack_eb90_test_frame(
            sun_present=0, alpha=-4.0, beta=8.0,
            adc_vax1=500, adc_vax2=600, adc_vay1=700, adc_vay2=800,
        )
        parser = EB90TestParser()

        parsed = parser.feed(frame_a + frame_b)

        self.assertEqual(len(parsed), 2)
        self.assertAlmostEqual(parsed[0].x, 1.25, places=3)
        self.assertAlmostEqual(parsed[0].y, 2.5, places=3)
        self.assertAlmostEqual(parsed[1].x, -4.0, places=3)

    def test_parser_rejects_invalid_sun_present(self):
        frame = bytearray(pack_eb90_test_frame(
            sun_present=1, alpha=0.0, beta=0.0,
            adc_vax1=0, adc_vax2=0, adc_vay1=0, adc_vay2=0,
        ))
        frame[0] = 0x05
        frame[-1] = sum(frame[:-1]) & 0xFF
        parser = EB90TestParser()

        parsed = parser.feed(bytes(frame))

        self.assertEqual(len(parsed), 0)
        self.assertEqual(parser.frame_error_count, 1)

    def test_parser_recovers_from_noise(self):
        good_frame = pack_eb90_test_frame(
            sun_present=1, alpha=5.0, beta=-2.0,
            adc_vax1=111, adc_vax2=222, adc_vay1=333, adc_vay2=444,
        )
        parser = EB90TestParser()

        parsed = parser.feed(b"\x00\x00\x00" + good_frame)

        self.assertEqual(len(parsed), 1)
        self.assertAlmostEqual(parsed[0].x, 5.0, places=3)
```

- [ ] **Step 3: 运行全部测试**

Run: `python tests\run_tests.py`

---

### Task 8: 运行完整验证

- [ ] **Step 1: 运行全部测试**

Run: `python tests\run_tests.py`

- [ ] **Step 2: 运行 GUI 验证模拟器模式**

Run: `python run_sun_gui.py`

验证清单：
- 选择 "Simulator: normal" + "EB90 18-byte (test)" 协议 → 点击 Connect → 应能看到 Alpha/Beta 曲线和数值
- 选择 "Simulator: normal" + "Recommended 32-byte" → 应仍能正常工作
- 选择 "Simulator: normal" + "EB90 26-byte (original)" → 应仍能正常工作
- 状态栏显示 "alpha=... beta=..." 而非 "x=... y=..."
- 曲线图标题显示 "Alpha/Beta" 而非 "X/Y"
- 数值面板标签显示 "alpha" / "beta" 而非 "x" / "y"

---

## 向后兼容性说明

| 场景 | 是否兼容 |
|---|---|
| 旧 26 字节 EB90 协议（真实太阳敏） | ✅ 完全保留，代码加注释未改动 |
| 推荐 32 字节协议（模拟器默认） | ✅ 完全保留 |
| 旧26字节模拟模式 | ✅ 完全保留（SimulatorThread 默认 protocol="recommended"） |
| 新 18 字节 EB90 测试协议 | 🆕 新增 |
| 18 字节模拟模式 | 🆕 新增 |
| CSV 记录 | ✅ 旧26字节格式 ADC 列写 0（默认值），新18字节写真实值 |
| SpotTelemetry ADC 字段 | ✅ 旧解析不填则默认 0，新解析器填真实值 |

---

## 已知限制

1. **18 字节无帧头**：帧同步依赖校验和 + `sun_present` 过滤（仅允许 0x00/0x01），误判概率约 1/12800。仅用于临时联调测试。
2. **`SimulatorThread` 的 `drop_frame` 模式**在 `eb90_test` 协议下：18 字节帧无 `seq` 字段，无法统计丢帧。CRC error 模式仍有效（翻转末字节校验和）。
3. **旧 `上位机代码修改建议_GLM.md` 中提到的两个 Bug**（`sun_simulator.py` `drop_frame` 多丢一帧、`sun_gui.py` 连接失败按钮不回滚）不在本次修改范围内，应另外修复。
4. **`EB90TestParser` 无 `_last_seq` 字段**，不统计丢帧数（`drop_count` 始终为 0），因为 18 字节帧没有序号字段。